<?php
/**
 * Настройки бэкенда. Скопируй этот файл рядом под именем config.php
 * и заполни. Сам config.php в репозиторий не попадает — в нём пароли.
 */
return [

    // Данные базы. Создаётся в панели Timeweb: «Базы данных» → «Создать».
    'db' => [
        'host' => 'localhost',
        'name' => '',
        'user' => '',
        'pass' => '',
    ],

    // Хэш пароля от админки. Как получить — см. README, раздел «Админка».
    // Обычный пароль сюда писать нельзя, только хэш.
    'admin_password_hash' => '',

    // Уведомления в Telegram. Необязательно: пусто — просто не отправляются.
    'telegram' => [
        'token'   => '',   // от @BotFather
        'chat_id' => '',   // куда слать, узнаётся у @userinfobot
    ],

    // Яндекс.Метрика. counter — номер счётчика, он же вписывается в js/main.js.
    // token нужен, только если хочешь видеть статистику прямо в админке.
    'metrika' => [
        'counter' => '',
        'token'   => '',
    ],
];
